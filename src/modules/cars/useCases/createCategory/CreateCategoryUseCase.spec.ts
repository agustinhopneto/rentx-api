describe('Create category', () => {
  it('should be able to sum', () => {
    const sum = 2 + 2;
    const result = 4;

    expect(sum).toBe(result);
  });

  it('should not be able to sum', () => {
    const sum = 2 + 2;
    const result = 5;

    expect(sum).not.toBe(result);
  });
});
